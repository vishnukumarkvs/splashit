apiVersion: apps/v1
kind: Deployment
metadata:
  name: unsplash-app
  labels:
    app: unsplash-app
spec:
  selector:
    matchLabels:
      app: unsplash-app
  template:
    metadata:
      labels:
        app: unsplash-app
    spec:
      containers:
      - name: unsplash-app
        image: docker.io/vishnukumarkvs/unsplash:v3
        ports:
          - containerPort: 8080
        env:
          - name: DB_USER
            value: postgres
          - name: DB_PWD
            value: password
          - name: DB_URL
            value: jdbc:postgresql://unsplash-postgres/postgres
          - name: QUEUE_URI
            value: https://sqs.us-west-2.amazonaws.com/895656015678/qq
          - name: AWS_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: mysecret
                key: accesskey
          - name: AWS_SECRET_KEY
            valueFrom:
              secretKeyRef:
                name: mysecret
                key: secretkey
